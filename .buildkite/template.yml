name: "Starter pipeline"
description: "An example pipeline for launching a rocket that shows how Buildkite works."
emoji: ":rocket:"
steps:
  - command: "buildkite-agent pipeline upload"
    label: ":pipeline:?"
